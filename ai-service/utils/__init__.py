"""AgroMind AI — Shared utility functions.

This package contains helper modules used across the training pipeline:
  • config.py      — Central configuration (paths, hyperparameters)
  • helpers.py     — Logging, class-weight calculation, metadata saving
"""
